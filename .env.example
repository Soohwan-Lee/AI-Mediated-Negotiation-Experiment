# LLM. Without a key, AI turns fall back to canned text so the flow is walkable.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol

# Developer / mockup panel. Always on under `next dev`. Set to 1 to keep it in
# a deployed PREVIEW build. LEAVE THIS UNSET FOR THE REAL STUDY — the panel
# names conditions and reveals the assignment, so it must not reach Prolific.
# NEXT_PUBLIC_DEV_TOOLS=1

# Supabase (planned — not yet wired; see docs/DATA_MODEL.md)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
